
import React from 'react';

// This component is no longer needed as the text selection popup 
// will appear directly above selected text in the main content area
const TextSelectionArea: React.FC = () => {
  return null;
};

export default TextSelectionArea;
